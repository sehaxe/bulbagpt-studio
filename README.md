Отличный выбор! **Python 3.11** — это идеальный баланс: он уже получил все оптимизации скорости, но при этом все библиотеки (PyTorch, Gradio, Rust-биндинги) на нем работают стабильно.

Вот **финальные файлы**, обновленные под Python 3.11.

Я также подготовил правильный `requirements.txt`, которого раньше не было в переписке, но который необходим для инструкции.

---

### 1. 📄 `README.md` (Финальная версия)

Здесь везде прописан `python=3.11`, добавлена информация про поддержку Mac и Linux, а также про новый движок данных.

```markdown
# 🇧🇾 BulbaGPT Studio: Scientific AI Foundry

**BulbaGPT Studio** — это профессиональная кроссплатформенная среда для создания, обучения и экспорта малых языковых моделей (SLM) с кастомной архитектурой **Llama-3 (GQA)**, оптимизированная для **славянских языков (RU/BY)**.

Мы переписали ядро на **Rust**, чтобы устранить бутылочные горлышки в памяти, скорости ввода-вывода (IO) и качестве перемешивания данных.

> *"Это комбайн, который выжимает 100% из NVIDIA GPU и Apple Silicon (Metal) для создания моделей, готовых к продакшену."*

---

## 🧬 Технологии

### 1. Rust Data Engine v2.0
Ядро обработки данных полностью переписано для стабильности обучения:
*   **Hyper-Speed Shuffle:** Rust перемешивает гигабайты данных за секунды, используя многопоточность (`Rayon`) и `mmap`. Это устраняет "скачки Loss" при обучении на разнородных текстах.
*   **Parallel Tokenization:** Максимальная утилизация CPU при подготовке датасета.

### 2. Cross-Platform Training
*   🟢 **NVIDIA (Linux/WSL):** Поддержка `Flash Attention 2`, `Torch Compile`, 8-bit оптимизаторов (`bitsandbytes`).
*   🍎 **Apple Silicon (macOS):** Нативная поддержка **Metal (MPS)**. Обучайте модели прямо на MacBook M1/M2/M3.

### 3. Native GGUF Pipeline
Встроенная система **Hot Patching** автоматически создает `.gguf` файлы для использования в LM Studio или Ollama.

---

## 🧰 Инструменты

*   `main.py` — Графический интерфейс студии.
*   `dataset.py` — "Архитектор": генератор датасетов со стримингом (не ест RAM) и умным микшированием доменов.
*   `test.py` — "Инспектор": чат с обученной моделью в терминале.

---

## 🛠 Установка

### Вариант А: Linux / WSL2 (Windows)
*Идеально для видеокарт NVIDIA (RTX 3060, 4090, A100).*

1.  **Создайте окружение (Conda):**
    Мы используем **Python 3.11** для максимальной производительности.
    ```bash
    conda create -n bulba python=3.11
    conda activate bulba
    ```
2.  **Установите Rust:**
    ```bash
    curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
    source $HOME/.cargo/env
    ```
3.  **Соберите ядро (Обязательно):**
    ```bash
    pip install maturin
    cd bulba_rust
    maturin develop --release
    cd ..
    ```
4.  **Установите библиотеки:**
    ```bash
    pip install -r requirements.txt
    ```

### Вариант Б: macOS (Apple Silicon M1/M2/M3)
*Обучение на локальном Mac. Работает медленнее NVIDIA, но полностью функционально.*

1.  **Установите системные зависимости (Homebrew):**
    ```bash
    brew install rust
    brew install --cask miniconda
    ```
2.  **Настройте окружение:**
    ```bash
    conda create -n bulba python=3.11
    conda activate bulba
    ```
3.  **Соберите ядро:**
    ```bash
    pip install maturin
    cd bulba_rust
    maturin develop --release
    cd ..
    ```
4.  **Установите библиотеки:**
    ```bash
    # PyTorch Nightly нужен для лучших оптимизаций Metal (MPS)
    pip install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cpu
    pip install -r requirements.txt
    ```

---

## 🚀 Как пользоваться

### 1. Подготовка данных
Используйте `dataset.py`, чтобы собрать ваши `.txt`, `.json` или Parquet файлы в один большой, хорошо перемешанный корпус:
```bash
python dataset.py
```

### 2. Запуск студии
```bash
python main.py
```
1.  Откройте ссылку в браузере (обычно `http://127.0.0.1:7860`).
2.  Нажмите **"1. Prepare Data & Tokenizer"**. Rust токенизирует и перемешает данные.
3.  Выберите пресет (например, **AIst 150M**) и нажмите **"2. START"**.

### 3. Экспорт
Когда обучение завершено:
1.  Перейдите во вкладку **Export**.
2.  Нажмите **"Convert GGUF"**.
3.  Забирайте файл из папки `output_models` и запускайте в LM Studio!

---

License: MIT  
**BulbaGPT Project** 🇧🇾 — Science meets Open Source.